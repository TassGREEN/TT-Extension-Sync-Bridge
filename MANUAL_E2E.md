# 手工端到端验证

全程不要打开或检查聊天、摘要、聊天 metadata。

## 准备

1. 两台设备均安装相同版本 TT、Bridge 和目标插件。
2. TT 同步数据集启用 `extensions.store`。
3. 两端敏感凭据各自保留，不记录其值。
4. 先备份当前 TT 用户数据目录。

## A → B

1. 设备 A 在四个目标插件中设置可识别的非敏感测试偏好。
2. A 点击 Bridge“立即采集”，记录 UI 显示的每项 hash、revision、时间和设备 ID。
3. A 执行 TT 上传同步。
4. B 执行 TT 下载同步并重新加载 TT。
5. B 点击“恢复前预览”；确认没有未知版本、hash 错误或同名异 ID 冲突。
6. 对需人工确认的项目执行确认恢复。
7. 验证三个固定 ID 脚本、数据库配置、API 管理器非敏感配置、梦境创客、st-chatu8 标准设置及手工标签；确认已安装词表和图片记录未被替换。
8. 再次恢复同一快照，确认全部为 `noop` 且无重复脚本。
9. 确认 B 原有 API Key/Token/URL/账号值未被 A 覆盖。

## B → A 往返

1. B 修改一个非敏感测试偏好并立即采集。
2. B 上传，A 下载并重新加载。
3. A 预览并恢复。
4. A 再次采集；比较双方每个 adapter 的 `nonSensitiveHash`，应一致；`contentHash` 仍可因两端脱敏字段拓扑不同而不同。
5. 重复恢复，确认无重复记录或副作用。

## 失败路径

- 临时禁用一个目标插件：Bridge 应保留快照并报告 `missing-target`。
- 制造同名异 ID 的测试脚本：应报告硬冲突且不写入。
- 使用不受支持的插件版本：应报告 `incompatible`。
- 修改测试快照副本但不更新 hash：应在写入前拒绝。

不要通过编辑真实同步快照做破坏性测试；使用隔离副本或自动化测试 fixture。
